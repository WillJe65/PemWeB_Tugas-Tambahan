# ANALISIS CODE

- Jika ingin menampung metadata seperti file package.json, kita dapat menggunakan setup.py dengan librarly setup jadi cukup install semua package didalam setup.py diterminal dengan perintah via 

```pip install -e .```

- saat melakukan penginstallan, setup akan membuat folder tutorial.egg-info, semacam metadata untuk proyek yang sedang kita buat.

